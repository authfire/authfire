# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
permissions:
  contents: read
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - name: Build
        run: pnpm install --frozen-lockfile && pnpm run build
        env:
          NEXT_PUBLIC_FIREBASE_API_KEY: ${{ vars.NEXT_PUBLIC_FIREBASE_API_KEY }}
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: ${{ vars.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN }}
          NEXT_PUBLIC_FIREBASE_PROJECT_ID: ${{ vars.NEXT_PUBLIC_FIREBASE_PROJECT_ID }}
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: ${{ vars.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET }}
          NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: ${{ vars.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID }}
          NEXT_PUBLIC_FIREBASE_APP_ID: ${{ vars.NEXT_PUBLIC_FIREBASE_APP_ID }}
          NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID: ${{ vars.NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID }}
          NEXT_PUBLIC_HOSTNAME: ${{ vars.NEXT_PUBLIC_HOSTNAME }}
          NEXT_PUBLIC_APP_NAME: ${{ vars.NEXT_PUBLIC_APP_NAME }}
          NEXT_PUBLIC_LOGO_IMAGE_URL: ${{ vars.NEXT_PUBLIC_LOGO_IMAGE_URL }}
          NEXT_PUBLIC_LOGO_DARK_IMAGE_URL: ${{ vars.NEXT_PUBLIC_LOGO_DARK_IMAGE_URL }}
          NEXT_PUBLIC_PRIVACY_POLICY_URL: ${{ vars.NEXT_PUBLIC_PRIVACY_POLICY_URL }}
          NEXT_PUBLIC_TERMS_OF_SERVICE_URL: ${{ vars.NEXT_PUBLIC_TERMS_OF_SERVICE_URL }}
          NEXT_PUBLIC_ID_TOKEN_VERIFICATION_URL: ${{ vars.NEXT_PUBLIC_ID_TOKEN_VERIFICATION_URL }}
          NEXT_PUBLIC_SERVER_SIGN_OUT_URL: ${{ vars.NEXT_PUBLIC_SERVER_SIGN_OUT_URL }}
          NEXT_PUBLIC_LEGAL_BUSINESS_NAME: ${{ vars.NEXT_PUBLIC_LEGAL_BUSINESS_NAME }}
          NEXT_THEME_PRIMARY_COLOR: ${{ vars.NEXT_THEME_PRIMARY_COLOR }}
          NEXT_THEME_PRIMARY_FOREGROUND_COLOR: ${{ vars.NEXT_THEME_PRIMARY_FOREGROUND_COLOR }}
          NEXT_PUBLIC_RECAPTCHA_SITE_KEY: ${{ vars.NEXT_PUBLIC_RECAPTCHA_SITE_KEY }}
          NEXT_PUBLIC_OPENID_CONNECT_PROVIDER_ID: ${{ vars.NEXT_PUBLIC_OPENID_CONNECT_PROVIDER_ID }}
          NEXT_PUBLIC_OPENID_CONNECT_NAME: ${{ vars.NEXT_PUBLIC_OPENID_CONNECT_NAME }}
          NEXT_PUBLIC_OPENID_CONNECT_LOGO_URL: ${{ vars.NEXT_PUBLIC_OPENID_CONNECT_LOGO_URL }}
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          channelId: live
          projectId: ${{ vars.FIREBASE_PROJECT_ID }}
